--- a/etc/init.d/kexec-load.orig	2016-06-03 13:11:42.613228507 +0000
+++ b/etc/init.d/kexec-load	2016-06-03 13:11:50.041037426 +0000
@@ -106,14 +106,6 @@
 	exit 3
 	;;
   stop)
-	# If running systemd, we want kexec reboot only if current
-	# command is reboot
-	if [ -d /run/systemd/system ]; then
-		systemctl list-jobs systemd-reboot.service | grep -q systemd-reboot.service
-		if [ $? -ne 0 ]; then
-			exit 0
-		fi
-	fi
 	do_stop
 	;;
   *)
