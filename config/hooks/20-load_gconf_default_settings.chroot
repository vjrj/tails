#!/bin/sh

set -e

# Load GConf settings.

echo "Loading GConf settings"

gct() {
	gconftool-2 \
	   --direct \
	   --config-source xml:readwrite:/etc/gconf/gconf.xml.defaults \
	   "${@}"
}

for file in /root/config/tails/gconf/*.xml ; do
	gct --load "${file}"
done
