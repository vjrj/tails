   <div class="tip">
   <p>To accelerate the build, you can disable some languages by editing
   the <span class="code">po_slave_languages</span> parameter in the file
   <span class="filename">ikiwiki.setup</span>, or by using the <span class="code">--set</span> option. Adding <span class="code">--set po_slave_languages="  - fr|Français"</span> will build the website only in English and French.</p>
   </div>
