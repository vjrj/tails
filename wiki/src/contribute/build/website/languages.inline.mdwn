   <div class="tip">
   <p>To accelerate the build, you can enable only some languages.
   For example, you can add <span class="code">--set po_slave_languages="  - fr|Français"</span> to build the website only in English and French.</p>
   </div>
