[[!comment format=mdwn
 ip="127.0.0.1"
 subject="comment 9"
 date="2012-11-18T06:16:13Z"
 content="""
>Debian unix,

>Ubuntu unix

\"unix\"?!

1.) Linux =/= Unix

2.) Linux is the *kernel*, which is only a fraction of the *operating system* (OS), which is more properly called, \"**GNU**/Linux\", or \"**GNU**+Linux\".


3.) **GNU** is an acronym for *\"Gnu's Not Unix\"* (recursive)

4.) The full, official name of the Debian OS is actually \"*Debian GNU/Linux*\". (one of the few, if not the *only*, to avoid the error of using only \"Linux\") 
"""]]
