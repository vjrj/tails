[[!comment format=mdwn
 username="Tails"
 ip="127.0.0.1"
 subject="comment 4"
 date="2012-09-03T08:32:27Z"
 content="""
> but isn't this a consolidation of ssl info somewhat comparable to http everywhere's observatory if enabled (but it's disabled by default and it's opt-in)

It is.

> which would allow a hidden service admin to gather a lot of information on Tor users as, unless i'm mistaken, monkeysphere uses this one hidden service in its operation? 

In the current state of how the Monkeysphere XUL extension works,
it means a single entity (which happens to be a hidden service admin, though I don't see how it's relevant here) can gather information about what websites Tails users visit *that have a SSL certificate that cannot be validated by the Firefox \"normal\" CA-based process*.

"""]]
