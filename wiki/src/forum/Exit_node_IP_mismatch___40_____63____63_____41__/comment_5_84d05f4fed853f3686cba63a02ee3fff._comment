[[!comment format=mdwn
 ip="127.0.0.1"
 subject="comment 5"
 date="2013-01-07T02:48:30Z"
 content="""
>>You mean I might be end up using two exit nodes at the same time?

>Different TCP connections may use different circuits.

In case you missed it, that's a \"yes\"! ;o)

You'll frequently be using more than one circuit at a time. You also needn't be using the \"last\" circuit at all... yet... or, indeed *ever*

You can watch which circuits are being used for what connections in that Vidalia window.
"""]]
