[[!comment format=mdwn
 username="Tails"
 ip="127.0.0.1"
 subject="comment 1"
 date="2012-03-29T10:16:51Z"
 content="""
> Or is there like a route to the VM which causes a potential attacker to intercept all data?

No. The host OS' traffic will be routed the same no matter if Tails is run in a VM or not.
"""]]
