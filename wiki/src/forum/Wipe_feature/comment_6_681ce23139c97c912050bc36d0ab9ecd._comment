[[!comment format=mdwn
 ip="127.0.0.1"
 subject="comment 6"
 date="2012-07-24T12:38:43Z"
 content="""
>or even using DBAN with the \"PRNG Stream\" option set to only one round.

Doesn't that write zeros to the drive after the pseudo-random data?

In fact, doesn't DBAN do this with *all* of the methods it offers?

If so, DBAN would obviously not be a good choice for wiping a drive prior to encrypting it.
"""]]
