[[!comment format=mdwn
 username="Tails"
 ip="127.0.0.1"
 subject="comment 4"
 date="2012-05-02T08:36:51Z"
 content="""
nautilus-wipe lets you choose among different number of passes proposed by the secure-detele suite (1, 2, 35. It defaults to two pseudo-random (`/dev/urandom`) passes.

> 2.) /dev/random could take weeks to fill a drive, whereas /dev/urandom
> (pseudo-random) should take no more than \"mere\" days. (and /dev/zero would be
> done in a matter of hours, at most)

I'm curious about this claim. Did you reproduce this on a system with haveged running, such as Tails? I've never had to wait *days* for it to wipe a disk using `/dev/urandom`.

"""]]
