[[!comment format=mdwn
 username="Tails"
 ip="127.0.0.1"
 subject="comment 2"
 date="2012-10-11T00:06:12Z"
 content="""
In short, the MAC address is a *unique* identifier of your network interfaces (e.g. wired and wireless network cards). It is disclosed on the [[!wikipedia LAN]] you use, so:

* the operators of the network can correlate 
"""]]
