[[!comment format=mdwn
 ip="127.0.0.1"
 subject="Similar Problem"
 date="2012-12-29T03:17:08Z"
 content="""
Hi, I have a similar problem -- 0.15 boots fine from DVD, but no persistence allowed if you boot from DVD ( :( why?) Anyhow, I couldn't get the USB to boot (same ISO) until I used YUMI -- now it gets to the end of the progress bar and just sits there (both normal and failsafe modes).
"""]]
