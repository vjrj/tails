[[!comment format=mdwn
 username="Tails"
 ip="127.0.0.1"
 subject="comment 1"
 date="2012-04-23T10:35:32Z"
 content="""
IPv6 has already happened (at least if you ISP allows it). Tails currently blocks all IPv6 Internet traffic because Tor doesn't support it, but things are developing on that front. Whenever Tor has support for IPv6, Tails will utilize it.
"""]]
