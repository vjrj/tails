[[!comment format=mdwn
 username="Tails"
 ip="127.0.0.1"
 subject="comment 12"
 date="2012-09-30T13:26:24Z"
 content="""
> You can run **sudo ifconfig** (You will have to setup a root password at boot).

Either one of those commands will work to, and don't require root priviledges:

        /sbin/ifconfig
        ip addr

"""]]
