[[!comment format=mdwn
 ip="127.0.0.1"
 subject="comment 5"
 date="2012-10-05T08:36:07Z"
 content="""
>and there's much confusion among users about what that \"new identity\" thing means exactly (e.g. some seem to think it implies getting a new Exit Node as result).

That's what I've always took it to mean: a new exit node.

The \"New Identity\" function in **TorButton** (big green onion icon to the left of URL bar) also seems to clear all cookies.
"""]]
