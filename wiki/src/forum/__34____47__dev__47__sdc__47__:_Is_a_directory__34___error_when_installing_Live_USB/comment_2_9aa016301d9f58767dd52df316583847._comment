[[!comment format=mdwn
 username="Tails"
 ip="127.0.0.1"
 subject="comment 2"
 date="2012-04-09T07:52:13Z"
 content="""
Either `/dev/sdc` is an internal drive, and you're lucky the system makes it harder to overwrite its content.

Or it's the correct (external) drive, and the system is configured in a non-Ubuntu-standard way. In that case, try switching to root (`sudo su -`) and retry.

"""]]
