[[!comment format=mdwn
 username="Tails"
 ip="127.0.0.1"
 subject="comment 3"
 date="2012-12-08T19:00:21Z"
 content="""
The VirtualBox guest kernel modules don't load if not running in VirtualBox, so the userspace guest tools can't talk to the kernel modules, se (I guess) this error message is triggered. Can be safely ignored.

"""]]
