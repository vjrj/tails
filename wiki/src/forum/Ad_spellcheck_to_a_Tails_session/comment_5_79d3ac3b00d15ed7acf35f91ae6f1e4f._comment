[[!comment format=mdwn
 username="Tails"
 ip="127.0.0.1"
 subject="comment 5"
 date="2012-06-27T16:56:23Z"
 content="""
Adding a spellchecker in already in our [[todo list|/todo/spell-checker]] so that will come at some point.

For the time being, you can install the MySpell packages corresponding to the language of your choice.

If you are using a [[persistent volume|/doc/first_steps/persistence]] you can use it to store those packages between working sessions with its [[APT Packages|/doc/first_steps/persistence/configure#apt_packages]] feature.
"""]]
