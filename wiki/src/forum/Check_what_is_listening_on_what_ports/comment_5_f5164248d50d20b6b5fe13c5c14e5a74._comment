[[!comment format=mdwn
 ip="127.0.0.1"
 subject="comment 5"
 date="2013-02-25T03:43:27Z"
 content="""
Also, another way to see services listening on ports is:
`netstat -anp | grep -e tcp -e udp`

The output is obviously different from that of the commands the OP listed but, sadly, I don't know enough to understand the difference.
"""]]
