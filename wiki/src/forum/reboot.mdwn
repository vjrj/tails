When rebooting -after(browser)crash,automatic or kill in shell after crash,kill process(perl) in shell then crash- most custum settings persist.
Get normal relogin screen+-password.
Icons in toppanel are reorganized.

Normal behave?
