[[!comment format=mdwn
 username="Tails"
 ip="127.0.0.1"
 subject="comment 1"
 date="2012-09-28T06:57:30Z"
 content="""
Create it first, resize it afterwards.

The way persistence works is deliberately plain simple: one USB stick, one Tails, one persistence volume, one persona. Anything else will require extra work on your side.
"""]]
