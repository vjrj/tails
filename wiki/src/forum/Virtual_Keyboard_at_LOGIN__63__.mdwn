It doesn't seem to me as if there is any way to access the Florence Virutal Keyboard when creating a password at *login*.

I suppose the solution, if hardware keylogging is a concern, would be to change the password using Florence as soon as one is logged-in?
