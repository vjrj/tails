##**Open Synaptic and click:**

*Settings=>Preferences=>Network*

**you'll find it is set for a 'Direct connection to the internet' instead of a Manually configured connection which uses Tor.**

    Under 'Manual Proxy configuration', here are the following fields which
    are not configured in Tails:

    * HTTP Proxy : Port

    * FTP Proxy : Port

    * No Proxy For:

**how should this area in Synaptic be configured and will you configure it for the next stable release? ty**
