[[!comment format=mdwn
 ip="127.0.0.1"
 subject="comment 1"
 date="2012-08-10T10:07:46Z"
 content="""
As far as I know, the installed  packages are stored in the persistence (if correctly [[setup|doc/first_steps/persistence/configure]]). 

All you have to do is to reinstall them after each reboot. They are *not automatically reinstalled*. Note that it will work even offline.

Good luck
"""]]
