Any help resolving this issue would be nice. I'm using Tails 0.17.

Basically, I've successfully created the DVD, boot from the DVD, then get past the boot screen and into the greeter screen. I click "login" then I'm returned to a blank screen where I can only use the keyboard and nothing else.

