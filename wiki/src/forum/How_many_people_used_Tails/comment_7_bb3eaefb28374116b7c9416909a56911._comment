[[!comment format=mdwn
 ip="127.0.0.1"
 subject="comment 7"
 date="2012-09-03T23:30:58Z"
 content="""
> This feed is retrieved by the software used to warn users when they are using a version of Tails that is out of date regarding security issues.

So you ship an auto-update check that's on by default, which you also use to track users.

> The only thing we know from this is one instance of Tails has been booted somewhere and successfully connected to the Tor network.

That's called \"tracking\". You probably object when software that you install does the same thing without asking you first, but obviously, your toy is different, and you don't need permission from a user.
"""]]
