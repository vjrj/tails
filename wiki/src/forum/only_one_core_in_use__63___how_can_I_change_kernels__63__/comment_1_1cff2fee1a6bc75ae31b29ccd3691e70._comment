[[!comment format=mdwn
 username="Tails"
 ip="127.0.0.1"
 subject="comment 1"
 date="2012-09-27T16:32:19Z"
 content="""
> it only shows one cpu in use

This is [[being worked on|todo/nx_bit]].

> How may I update my kernel to one which supports more than one cpu?

You may want to try building a Tails image from the
`feature/multikernel` branch, or help us bringing it into
a releasable state.

"""]]
