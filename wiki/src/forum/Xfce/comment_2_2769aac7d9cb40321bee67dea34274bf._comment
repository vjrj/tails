[[!comment format=mdwn
 ip="127.0.0.1"
 subject="Excessive resource consumption by GNOME Shell/Fallback"
 date="2013-04-16T09:09:43Z"
 content="""
Compared to GNOME2, GNOME Shell uses excessive CPU and RAM resources even in fallback mode.

Likely there are many Tails users that own low- to mid-level hardware like Netbooks.

In my opinion, there should be a serious consideration of using XFCE4 instead GNOME for Tails.
"""]]
