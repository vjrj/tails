[[!comment format=mdwn
 ip="127.0.0.1"
 subject="NoScript should be blocking by default on Tails"
 date="2012-04-29T10:57:33Z"
 content="""
These exploits still all depend on scripts being enabled in the browser, am I right? Or not?

>>>>>>>> Set NoScript to block scripts globally folks!

This is the very first thing I do after booting Tails.  I still maintain it should be the default configuration. 
"""]]
