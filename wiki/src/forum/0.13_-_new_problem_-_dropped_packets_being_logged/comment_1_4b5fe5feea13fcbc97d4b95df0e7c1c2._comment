[[!comment format=mdwn
 username="Tails"
 ip="127.0.0.1"
 subject="comment 1"
 date="2012-09-21T09:27:57Z"
 content="""
> why the big change in iptables configuration

See the \"Local services whitelist\" section of the [[network filter design page|contribute/design/Tor_enforcement/Network_filter]] (and perhaps also the [[todo item|todo/firewall_lockdown/]]).

> why are these entries appearing in my logs?

To make it easier to detect programs that do not behave well, i.e. that have leaks.

> SRC=myIP DST=aTorNode DPT=443

Are you sure that you see traffic to Tor nodes being dropped? That's certainly not how it's supposed to be and suggests a bug ([[please report it|support/found_a_problem]]).

"""]]
