Hi,
is there a way to set the default language, country and keyboard layout during login?
When I manually select the language (Japanese) it also sets country and keyboard. Can the default (English) be changed?
