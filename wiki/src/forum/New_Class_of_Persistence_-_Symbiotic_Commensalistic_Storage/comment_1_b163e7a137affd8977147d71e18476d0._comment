[[!comment format=mdwn
 ip="127.0.0.1"
 subject="comment 1"
 date="2012-08-08T15:09:52Z"
 content="""
Or, you know, just get a bigger flash drive in the 32-64GB range instead of relying on consistently using the same computer while storing data that Tails uses onto it. In fact, such a system kind of defeats the point when an installed OS will overwrite the data it thinks is supposed to be deleted.
"""]]
