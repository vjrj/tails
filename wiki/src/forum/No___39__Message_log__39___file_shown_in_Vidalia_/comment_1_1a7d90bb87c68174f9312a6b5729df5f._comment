[[!comment format=mdwn
 username="Tails"
 ip="127.0.0.1"
 subject="comment 1"
 date="2012-08-10T10:35:46Z"
 content="""
The default settings in Vidalia makes it only show warnings, notices and errors, and if Tor issues none of them after Vidalia has started, it won't show anything. If you want to see something, just to verify that it works, send a HUP to Tor:

    sudo kill -HUP $(pidof tor)

"""]]
