[[!comment format=mdwn
 ip="127.0.0.1"
 subject="comment 3"
 date="2013-02-24T21:47:07Z"
 content="""
Oh no, sorry, I messed something up.  This should be correct:

In more detail: the names of the files you download and save should be:

* tails-i386-0.16.iso
* tails-i386-0.16.iso.pgp
* tails-signing.key

So the shell commands in Linux (Windows should be similar) should be:

    gpg --import tails-signing.key

    gpg --verify tails-i386-0.16.iso.pgp tails-i386-0.16.iso

"""]]
