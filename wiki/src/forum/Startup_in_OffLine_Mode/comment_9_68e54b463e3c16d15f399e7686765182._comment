[[!comment format=mdwn
 username="Tails"
 ip="127.0.0.1"
 subject="comment 9"
 date="2012-11-03T02:26:07Z"
 content="""
> This won't work as `net` isn't compiled as a module in the Debian kernel.

... and Tails is based on Debian (and specifically using Debian's kernel).
"""]]
