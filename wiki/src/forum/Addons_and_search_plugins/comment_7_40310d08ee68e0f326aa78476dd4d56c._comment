[[!comment format=mdwn
 username="alessandro.g89"
 ip="127.0.0.1"
 subject="comment 7"
 date="2012-07-30T14:17:30Z"
 content="""
I read that window size is detectable by the server, so if you resize to a non-standard one (like 986x665) your actions on different sites could be correlated by an attacker. In short, you are exposed to fingerprinting.

Zoom text should be safe, as it only affects local rendering of pages
"""]]
