[[!comment format=mdwn
 username="Tails"
 ip="127.0.0.1"
 subject="comment 3"
 date="2012-04-29T17:23:13Z"
 content="""
If you've enabled *APT Lists* and *APT Packages* persistence, then your persistent *volume* (not to be confused with your `Persistent` directory in home) shall contain the APT files you're looking for, respectively in the `apt/lists` and `apt/cache` sub-directories.
"""]]
