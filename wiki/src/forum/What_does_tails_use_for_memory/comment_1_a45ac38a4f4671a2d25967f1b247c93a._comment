[[!comment format=mdwn
 username="Tails"
 ip="127.0.0.1"
 subject="comment 1"
 date="2012-01-21T11:56:30Z"
 content="""
> Does tails just use the computers RAM, if so is it only the DRAM?

Yes. All modifications to the file system are only saved in RAM, except if you explicitly mounted some storage media (e.g. through the `Places` menu).

> Does it use any swap, hardisk swap?

[[No|contribute/design#index33h3]].
"""]]
