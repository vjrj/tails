[[!comment format=mdwn
 ip="127.0.0.1"
 subject="tcp dump"
 date="2012-09-12T09:51:52Z"
 content="""
\"\"I suggest using tcpdump, which already is installed in Tails, instead:
sudo tcpdump -i any -w tails.dump\"\"

Which version of of Tails has tcpdump, 0.12.1 or 13...?
I can't find it in 0.12.1.
"""]]
