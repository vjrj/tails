[[!comment format=mdwn
 username="anonym"
 ip="127.0.0.1"
 subject="comment 1"
 date="2010-07-20T19:49:23Z"
 content="""
At the moment you can only use it through the terminal. Just type the following:

sudo ifconfig eth0 down
sudo macchanger -a -e eth0
sudo ifconfig eth0 up

where eth0 is assumed to be the network interface with the Internet connection. 
"""]]
